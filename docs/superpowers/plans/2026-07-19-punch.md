# punch 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 출근/퇴근 원탭 기록 + 월 합계 + CSV/백업을 갖춘 서버리스 웹페이지를 만들어 GitHub Pages에 배포한다.

**Architecture:** 순수 로직(`punch.js`, ES 모듈)과 DOM 연결(`app.js`)을 분리한다. 로직은 `node --test`(Node 내장 테스트 러너)로 TDD하고, 화면은 로컬 서버로 수동 검증한다. 데이터는 localStorage 키 `punch:v1`에 기록 배열로 저장한다.

**Tech Stack:** HTML + CSS + 바닐라 JS(ES 모듈). 의존성 0, 빌드 도구 0. 테스트는 Node 내장 `node:test`. 배포는 GitHub Pages.

> 스펙은 "파일 1개"라 했지만, 로직을 테스트하려면 분리가 필요해 3파일(index.html / punch.js / app.js)로 한다. 스펙의 취지(프레임워크·빌드 없음)는 유지된다.

## Global Constraints

- 외부 의존성 금지: npm 패키지 0개, CDN 스크립트 0개, 빌드 단계 없음
- 서버 코드 금지: 정적 파일만. 데이터는 localStorage 키 `punch:v1`
- UI 문구는 한국어
- 모바일 우선: 폰 화면(375px)에서 먼저 보기 좋아야 함
- 기록 모델(하루 1건): `{ date: "YYYY-MM-DD", in: "HH:MM", out: "HH:MM"|null, overnight: boolean }`
- 정렬: 기록 배열은 항상 날짜 내림차순(최신이 위)
- 프로젝트 루트: `/Users/imina/Project/punch`
- 커밋 메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

## 파일 구조 (최종)

```
punch/
├── index.html        # 마크업 + CSS(인라인 <style>) — 화면 전부
├── punch.js          # 순수 로직 ES 모듈 (DOM/localStorage 접근 없음)
├── app.js            # DOM 연결 (이벤트, 렌더, localStorage 읽기/쓰기)
├── package.json      # {"type":"module"} — node --test용. 의존성 없음
├── README.md         # 사용법 + 폰 알람 팁 + 백업 안내
├── test/
│   ├── core.test.js      # 시간/기간 계산 테스트
│   ├── records.test.js   # 기록 CRUD/집계 테스트
│   └── export.test.js    # CSV/백업 테스트
└── docs/ (기존 설계 문서)
```

---

### Task 1: 프로젝트 뼈대 + 시간 계산 로직

**Files:**
- Create: `package.json`
- Create: `punch.js`
- Test: `test/core.test.js`

**Interfaces:**
- Produces (이후 모든 태스크가 사용):
  - `todayStr(now: Date): string` → `"2026-07-19"` (로컬 시간 기준)
  - `nowHM(now: Date): string` → `"09:02"`
  - `monthKey(dateStr: string): string` → `"2026-07"`
  - `shiftMonth(month: string, delta: number): string` → `shiftMonth("2026-07", -1) === "2026-06"`
  - `addDays(dateStr: string, delta: number): string` → `addDays("2026-08-01", -1) === "2026-07-31"`
  - `toMinutes(hm: string): number` → `"09:30"` → `570`
  - `durationMinutes(rec): number|null` — out 없으면 null, `overnight`면 +1440
  - `isOvernightCandidate(inHM, outHM): boolean` — out이 in보다 이르면 true
  - `formatDuration(min: number|null): string` → `503` → `"8h 23m"`, null → `""`

- [ ] **Step 1: package.json 생성**

```json
{
  "name": "punch",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test test/*.test.js" }
}
```

- [ ] **Step 2: 실패하는 테스트 작성** — `test/core.test.js`

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import * as P from '../punch.js';

test('todayStr: 로컬 날짜를 YYYY-MM-DD로', () => {
  assert.equal(P.todayStr(new Date(2026, 6, 19, 9, 2)), '2026-07-19');
  assert.equal(P.todayStr(new Date(2026, 0, 5)), '2026-01-05');
});

test('nowHM: HH:MM 0채움', () => {
  assert.equal(P.nowHM(new Date(2026, 6, 19, 9, 2)), '09:02');
  assert.equal(P.nowHM(new Date(2026, 6, 19, 18, 24)), '18:24');
});

test('monthKey / shiftMonth / addDays', () => {
  assert.equal(P.monthKey('2026-07-19'), '2026-07');
  assert.equal(P.shiftMonth('2026-07', -1), '2026-06');
  assert.equal(P.shiftMonth('2026-01', -1), '2025-12');
  assert.equal(P.shiftMonth('2026-12', 1), '2027-01');
  assert.equal(P.addDays('2026-08-01', -1), '2026-07-31');
  assert.equal(P.addDays('2026-07-19', 1), '2026-07-20');
});

test('toMinutes', () => {
  assert.equal(P.toMinutes('00:00'), 0);
  assert.equal(P.toMinutes('09:30'), 570);
});

test('durationMinutes: 일반 / 미퇴근 / 야근', () => {
  assert.equal(P.durationMinutes({ in: '09:01', out: '18:24', overnight: false }), 563);
  assert.equal(P.durationMinutes({ in: '09:01', out: null, overnight: false }), null);
  assert.equal(P.durationMinutes({ in: '22:00', out: '01:30', overnight: true }), 210);
});

test('isOvernightCandidate', () => {
  assert.equal(P.isOvernightCandidate('09:00', '18:00'), false);
  assert.equal(P.isOvernightCandidate('22:00', '01:30'), true);
});

test('formatDuration', () => {
  assert.equal(P.formatDuration(503), '8h 23m');
  assert.equal(P.formatDuration(60), '1h 00m');
  assert.equal(P.formatDuration(null), '');
});
```

- [ ] **Step 3: 테스트가 실패하는지 확인**

Run: `cd /Users/imina/Project/punch && node --test test/*.test.js`
Expected: FAIL — `Cannot find module '../punch.js'`

- [ ] **Step 4: 최소 구현** — `punch.js`

```js
// punch.js — 순수 로직 (DOM/localStorage 없음). node --test로 테스트한다.

export function todayStr(now) {
  const y = now.getFullYear();
  const m = String(now.getMonth() + 1).padStart(2, '0');
  const d = String(now.getDate()).padStart(2, '0');
  return `${y}-${m}-${d}`;
}

export function nowHM(now) {
  return `${String(now.getHours()).padStart(2, '0')}:${String(now.getMinutes()).padStart(2, '0')}`;
}

export function monthKey(dateStr) {
  return dateStr.slice(0, 7);
}

export function shiftMonth(month, delta) {
  const [y, m] = month.split('-').map(Number);
  const d = new Date(y, m - 1 + delta, 1);
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`;
}

export function addDays(dateStr, delta) {
  const [y, m, d] = dateStr.split('-').map(Number);
  return todayStr(new Date(y, m - 1, d + delta));
}

export function toMinutes(hm) {
  const [h, m] = hm.split(':').map(Number);
  return h * 60 + m;
}

export function durationMinutes(rec) {
  if (!rec.in || !rec.out) return null;
  let diff = toMinutes(rec.out) - toMinutes(rec.in);
  if (rec.overnight) diff += 1440; // 자정 넘긴 야근
  return diff;
}

export function isOvernightCandidate(inHM, outHM) {
  return toMinutes(outHM) < toMinutes(inHM);
}

export function formatDuration(min) {
  if (min == null) return '';
  return `${Math.floor(min / 60)}h ${String(min % 60).padStart(2, '0')}m`;
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test test/*.test.js`
Expected: PASS — 7 tests

- [ ] **Step 6: 커밋**

```bash
git add package.json punch.js test/core.test.js
git commit -m "feat: 시간/기간 계산 순수 로직 + 테스트"
```

---

### Task 2: 기록 CRUD + 집계 로직

**Files:**
- Modify: `punch.js` (끝에 추가)
- Test: `test/records.test.js`

**Interfaces:**
- Consumes: Task 1의 `todayStr`, `addDays`, `durationMinutes`
- Produces (app.js가 사용):
  - `getRecord(records, date): rec|null`
  - `todayState(records, date): 'none'|'in'|'done'`
  - `activeRecord(records, today): rec|null` — **오늘 또는 어제**의 미퇴근(out==null) 기록. 자정 넘긴 퇴근 처리의 핵심
  - `punchIn(records, date, hm): records` — 이미 있으면 throw
  - `punchOut(records, date, hm, overnight=false): records` — 미출근이거나 이미 퇴근이면 throw
  - `upsertRecord(records, rec): records` — 같은 날짜 교체(수정) 또는 추가
  - `deleteRecord(records, date): records`
  - `monthRecords(records, month): records` — 해당 월만, 내림차순 유지
  - `monthTotalMinutes(records, month): number` — 미퇴근 기록은 제외
  - `missingOut(rec, today): boolean` — 지난 날짜인데 퇴근 미입력

- [ ] **Step 1: 실패하는 테스트 작성** — `test/records.test.js`

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import * as P from '../punch.js';

const R = (date, inHM, out = null, overnight = false) => ({ date, in: inHM, out, overnight });

test('punchIn: 추가 + 내림차순 정렬, 중복 출근은 throw', () => {
  let recs = P.punchIn([], '2026-07-18', '09:01');
  recs = P.punchIn(recs, '2026-07-19', '08:55');
  assert.deepEqual(recs.map(r => r.date), ['2026-07-19', '2026-07-18']);
  assert.deepEqual(recs[0], R('2026-07-19', '08:55'));
  assert.throws(() => P.punchIn(recs, '2026-07-19', '10:00'));
});

test('punchOut: 퇴근 기록, 상태 오류는 throw', () => {
  let recs = P.punchIn([], '2026-07-19', '09:00');
  recs = P.punchOut(recs, '2026-07-19', '18:30');
  assert.equal(recs[0].out, '18:30');
  assert.throws(() => P.punchOut(recs, '2026-07-19', '19:00')); // 이미 퇴근
  assert.throws(() => P.punchOut(recs, '2026-07-20', '19:00')); // 미출근
});

test('punchOut: overnight 플래그', () => {
  let recs = P.punchIn([], '2026-07-18', '22:00');
  recs = P.punchOut(recs, '2026-07-18', '01:30', true);
  assert.equal(recs[0].overnight, true);
  assert.equal(P.durationMinutes(recs[0]), 210);
});

test('todayState', () => {
  assert.equal(P.todayState([], '2026-07-19'), 'none');
  const open = [R('2026-07-19', '09:00')];
  assert.equal(P.todayState(open, '2026-07-19'), 'in');
  const done = [R('2026-07-19', '09:00', '18:00')];
  assert.equal(P.todayState(done, '2026-07-19'), 'done');
});

test('activeRecord: 오늘 또는 어제의 미퇴근 기록', () => {
  assert.equal(P.activeRecord([], '2026-07-19'), null);
  const todayOpen = [R('2026-07-19', '09:00')];
  assert.equal(P.activeRecord(todayOpen, '2026-07-19').date, '2026-07-19');
  const yesterdayOpen = [R('2026-07-18', '22:00')];
  assert.equal(P.activeRecord(yesterdayOpen, '2026-07-19').date, '2026-07-18');
  const tooOld = [R('2026-07-15', '09:00')];
  assert.equal(P.activeRecord(tooOld, '2026-07-19'), null); // 그제 이전은 수동 수정 대상
  const closed = [R('2026-07-19', '09:00', '18:00')];
  assert.equal(P.activeRecord(closed, '2026-07-19'), null);
});

test('upsertRecord: 수정과 추가, 정렬 유지', () => {
  let recs = [R('2026-07-19', '09:00', '18:00'), R('2026-07-17', '09:00', '18:00')];
  recs = P.upsertRecord(recs, R('2026-07-18', '10:00', '19:00')); // 추가
  assert.deepEqual(recs.map(r => r.date), ['2026-07-19', '2026-07-18', '2026-07-17']);
  recs = P.upsertRecord(recs, R('2026-07-19', '08:30', '17:30')); // 교체
  assert.equal(recs.length, 3);
  assert.equal(recs[0].in, '08:30');
});

test('deleteRecord', () => {
  const recs = [R('2026-07-19', '09:00', '18:00')];
  assert.deepEqual(P.deleteRecord(recs, '2026-07-19'), []);
});

test('monthRecords / monthTotalMinutes: 미퇴근 제외', () => {
  const recs = [
    R('2026-08-01', '09:00', '18:00'),          // 다른 달 (540)
    R('2026-07-19', '09:00'),                   // 미퇴근 → 합계 제외
    R('2026-07-18', '09:00', '18:00'),          // 540
    R('2026-07-17', '22:00', '01:00', true),    // 야근 180
  ];
  assert.equal(P.monthRecords(recs, '2026-07').length, 3);
  assert.equal(P.monthTotalMinutes(recs, '2026-07'), 720);
});

test('missingOut: 지난 날짜의 퇴근 미입력만 true', () => {
  assert.equal(P.missingOut(R('2026-07-18', '09:00'), '2026-07-19'), true);
  assert.equal(P.missingOut(R('2026-07-19', '09:00'), '2026-07-19'), false); // 오늘은 아직 근무 중
  assert.equal(P.missingOut(R('2026-07-18', '09:00', '18:00'), '2026-07-19'), false);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test test/*.test.js`
Expected: FAIL — `P.punchIn is not a function` (core.test.js 7개는 계속 PASS)

- [ ] **Step 3: 구현** — `punch.js` 끝에 추가

```js
// ---- 기록 CRUD / 집계 ----

function sortRecords(records) {
  return [...records].sort((a, b) => b.date.localeCompare(a.date)); // 최신이 위
}

export function getRecord(records, date) {
  return records.find(r => r.date === date) ?? null;
}

export function todayState(records, date) {
  const r = getRecord(records, date);
  if (!r || !r.in) return 'none';
  return r.out ? 'done' : 'in';
}

export function activeRecord(records, today) {
  const yesterday = addDays(today, -1);
  return records.find(r => r.out == null && (r.date === today || r.date === yesterday)) ?? null;
}

export function punchIn(records, date, hm) {
  if (getRecord(records, date)) throw new Error(`${date}에 이미 기록이 있습니다`);
  return sortRecords([...records, { date, in: hm, out: null, overnight: false }]);
}

export function punchOut(records, date, hm, overnight = false) {
  const r = getRecord(records, date);
  if (!r || r.out) throw new Error(`${date}는 퇴근을 기록할 상태가 아닙니다`);
  return records.map(x => (x.date === date ? { ...x, out: hm, overnight } : x));
}

export function upsertRecord(records, rec) {
  const rest = records.filter(r => r.date !== rec.date);
  return sortRecords([...rest, { out: null, overnight: false, ...rec }]);
}

export function deleteRecord(records, date) {
  return records.filter(r => r.date !== date);
}

export function monthRecords(records, month) {
  return records.filter(r => r.date.startsWith(month));
}

export function monthTotalMinutes(records, month) {
  return monthRecords(records, month)
    .map(durationMinutes)
    .filter(m => m != null)
    .reduce((a, b) => a + b, 0);
}

export function missingOut(rec, today) {
  return rec.out == null && rec.date < today;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/*.test.js`
Expected: PASS — 16 tests

- [ ] **Step 5: 커밋**

```bash
git add punch.js test/records.test.js
git commit -m "feat: 기록 CRUD와 월 집계 로직"
```

---

### Task 3: CSV / 백업 로직

**Files:**
- Modify: `punch.js` (끝에 추가)
- Test: `test/export.test.js`

**Interfaces:**
- Consumes: Task 1의 `durationMinutes`, Task 2의 `sortRecords`(내부)
- Produces (app.js가 사용):
  - `toCSV(records): string` — 헤더 `date,in,out,minutes`, 날짜 오름차순(엑셀 보기 편하게), 미퇴근은 out/minutes 빈칸
  - `toBackup(records): string` — `{"app":"punch","version":1,"records":[...]}` JSON
  - `fromBackup(text): records` — 형식 검증(불통과 시 한국어 메시지로 throw), 내림차순 정렬해 반환

- [ ] **Step 1: 실패하는 테스트 작성** — `test/export.test.js`

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import * as P from '../punch.js';

const recs = [
  { date: '2026-07-19', in: '09:00', out: null, overnight: false },
  { date: '2026-07-18', in: '09:01', out: '18:24', overnight: false },
];

test('toCSV: 헤더 + 오름차순 + 미퇴근 빈칸', () => {
  assert.equal(
    P.toCSV(recs),
    'date,in,out,minutes\n2026-07-18,09:01,18:24,563\n2026-07-19,09:00,,\n'
  );
});

test('백업 왕복: toBackup → fromBackup', () => {
  const restored = P.fromBackup(P.toBackup(recs));
  assert.deepEqual(restored, recs);
});

test('fromBackup: 형식 오류는 throw', () => {
  assert.throws(() => P.fromBackup('{{{'), /JSON/);
  assert.throws(() => P.fromBackup('{"app":"other","records":[]}'), /punch/);
  assert.throws(() =>
    P.fromBackup(JSON.stringify({ app: 'punch', version: 1, records: [{ date: 'bad', in: '09:00' }] }))
  );
  assert.throws(() =>
    P.fromBackup(JSON.stringify({ app: 'punch', version: 1, records: [{ date: '2026-07-19', in: '9시' }] }))
  );
});

test('fromBackup: 정렬 안 된 입력도 내림차순으로', () => {
  const messy = JSON.stringify({
    app: 'punch', version: 1,
    records: [
      { date: '2026-07-17', in: '09:00', out: '18:00' },
      { date: '2026-07-19', in: '09:00' },
    ],
  });
  assert.deepEqual(P.fromBackup(messy).map(r => r.date), ['2026-07-19', '2026-07-17']);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test test/*.test.js`
Expected: FAIL — `P.toCSV is not a function` (기존 16개는 PASS)

- [ ] **Step 3: 구현** — `punch.js` 끝에 추가

```js
// ---- CSV / 백업 ----

export function toCSV(records) {
  const rows = [...records]
    .sort((a, b) => a.date.localeCompare(b.date))
    .map(r => [r.date, r.in, r.out ?? '', durationMinutes(r) ?? ''].join(','));
  return ['date,in,out,minutes', ...rows].join('\n') + '\n';
}

export function toBackup(records) {
  return JSON.stringify({ app: 'punch', version: 1, records }, null, 2);
}

export function fromBackup(text) {
  let obj;
  try {
    obj = JSON.parse(text);
  } catch {
    throw new Error('JSON 파일이 아닙니다');
  }
  if (obj?.app !== 'punch' || !Array.isArray(obj.records)) {
    throw new Error('punch 백업 파일이 아닙니다');
  }
  for (const r of obj.records) {
    const ok =
      /^\d{4}-\d{2}-\d{2}$/.test(r?.date ?? '') &&
      /^\d{2}:\d{2}$/.test(r?.in ?? '') &&
      (r.out == null || /^\d{2}:\d{2}$/.test(r.out));
    if (!ok) throw new Error(`잘못된 기록이 있습니다: ${JSON.stringify(r)}`);
  }
  return sortRecords(
    obj.records.map(r => ({ date: r.date, in: r.in, out: r.out ?? null, overnight: !!r.overnight }))
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/*.test.js`
Expected: PASS — 20 tests

- [ ] **Step 5: 커밋**

```bash
git add punch.js test/export.test.js
git commit -m "feat: CSV 내보내기와 JSON 백업/복원 로직"
```

---

### Task 4: 화면 마크업 + 스타일

> **디자인 방침 (사용자 결정):** 이 태스크는 frontend-design 스킬을 적용해 구현한다.
> 아래 HTML/CSS는 **구조와 id 계약의 기준**이며, 시각 스타일(타이포그래피, 색, 여백,
> 버튼 질감, 상태 전환 느낌)은 스킬을 적용해 밋밋한 도구 느낌을 넘는 완성도로 끌어올릴 것.
> 단, 제약 유지: 외부 폰트/CDN 금지(시스템 폰트 스택), 의존성 0, id/name 계약 변경 금지,
> 375px 모바일 우선, 한국어 문구 유지.

**Files:**
- Create: `index.html`

**Interfaces:**
- Produces (app.js가 `getElementById`로 사용하는 id들 — 이름 변경 금지):
  `today-title`, `today-sub`, `punch-btn`, `undo-bar`, `undo-btn`,
  `prev-month`, `month-title`, `next-month`, `record-list`, `month-total`,
  `add-btn`, `csv-btn`, `backup-btn`, `restore-btn`, `restore-input`,
  `edit-dialog`, `edit-form`, `edit-title`, `edit-delete`
- `edit-form`의 필드 name: `date`(type=date, required), `in`(type=time, required), `out`(type=time), `overnight`(checkbox)

- [ ] **Step 1: index.html 작성**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>punch — 출퇴근 기록</title>
<style>
  :root {
    --accent: #2563eb;
    --out: #d97706;
    --done: #6b7280;
    --miss: #dc2626;
    --line: #e5e7eb;
  }
  * { box-sizing: border-box; margin: 0; }
  body {
    font-family: -apple-system, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
    background: #f9fafb; color: #111827;
    display: flex; justify-content: center;
    padding: 1rem;
  }
  main { width: 100%; max-width: 28rem; }
  header { text-align: center; margin: 0.5rem 0 1rem; }
  header h1 { font-size: 1.25rem; }
  #today-sub { color: var(--done); font-size: 0.9rem; min-height: 1.2em; margin-top: 0.25rem; }

  #punch-btn {
    width: 100%; height: 7rem; border: 0; border-radius: 1rem;
    font-size: 1.75rem; font-weight: 700; color: #fff;
    cursor: pointer; -webkit-tap-highlight-color: transparent;
  }
  #punch-btn.in { background: var(--accent); }
  #punch-btn.out { background: var(--out); }
  #punch-btn.done { background: var(--done); font-size: 1.1rem; cursor: default; }
  #punch-btn:active:not(:disabled) { transform: scale(0.98); }

  #undo-bar {
    display: flex; justify-content: space-between; align-items: center;
    margin-top: 0.5rem; padding: 0.5rem 0.75rem;
    background: #111827; color: #fff; border-radius: 0.5rem; font-size: 0.9rem;
  }
  #undo-btn { background: none; border: 0; color: #93c5fd; font-weight: 700; cursor: pointer; }

  section { margin-top: 1.5rem; }
  .month-nav { display: flex; justify-content: space-between; align-items: center; }
  .month-nav h2 { font-size: 1rem; }
  .month-nav button {
    width: 2.5rem; height: 2.5rem; border: 1px solid var(--line);
    background: #fff; border-radius: 0.5rem; cursor: pointer;
  }

  #record-list { list-style: none; padding: 0; margin: 0.75rem 0; }
  #record-list li {
    display: flex; justify-content: space-between; gap: 0.5rem;
    padding: 0.7rem 0.5rem; border-bottom: 1px solid var(--line);
    cursor: pointer; font-variant-numeric: tabular-nums;
  }
  #record-list .d { color: var(--done); width: 3rem; }
  #record-list .t { flex: 1; }
  #record-list .dur { font-weight: 600; }
  #record-list .miss { color: var(--miss); font-style: normal; font-size: 0.85rem; }

  #month-total { text-align: right; font-weight: 700; padding: 0.5rem; }

  footer { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 1rem; }
  footer button {
    flex: 1; min-width: 7rem; padding: 0.6rem; font-size: 0.85rem;
    border: 1px solid var(--line); background: #fff; border-radius: 0.5rem; cursor: pointer;
  }

  dialog {
    border: 0; border-radius: 1rem; padding: 1.25rem; width: min(20rem, 90vw);
  }
  dialog::backdrop { background: rgb(0 0 0 / 0.4); }
  #edit-form h3 { margin-bottom: 1rem; }
  #edit-form label { display: block; margin-bottom: 0.75rem; font-size: 0.9rem; }
  #edit-form input:not([type="checkbox"]) {
    display: block; width: 100%; margin-top: 0.25rem;
    padding: 0.5rem; border: 1px solid var(--line); border-radius: 0.5rem; font-size: 1rem;
  }
  #edit-form menu {
    display: flex; gap: 0.5rem; justify-content: flex-end; padding: 0; margin-top: 1rem;
  }
  #edit-form menu button {
    padding: 0.5rem 1rem; border-radius: 0.5rem; border: 1px solid var(--line);
    background: #fff; cursor: pointer;
  }
  #edit-save { background: var(--accent); color: #fff; border: 0; }
  #edit-delete { color: var(--miss); margin-right: auto; }
</style>
</head>
<body>
<main>
  <header>
    <h1 id="today-title">오늘</h1>
    <p id="today-sub"></p>
  </header>

  <button id="punch-btn" class="in">출근 🌅</button>
  <div id="undo-bar" hidden>
    <span>기록됨</span>
    <button id="undo-btn">실행취소</button>
  </div>

  <section>
    <div class="month-nav">
      <button id="prev-month" aria-label="이전 달">←</button>
      <h2 id="month-title"></h2>
      <button id="next-month" aria-label="다음 달">→</button>
    </div>
    <ul id="record-list"></ul>
    <p id="month-total"></p>
  </section>

  <footer>
    <button id="add-btn">+ 기록 추가</button>
    <button id="csv-btn">CSV 내보내기</button>
    <button id="backup-btn">백업</button>
    <button id="restore-btn">복원</button>
    <input type="file" id="restore-input" accept="application/json,.json" hidden>
  </footer>
</main>

<dialog id="edit-dialog">
  <form method="dialog" id="edit-form">
    <h3 id="edit-title">기록 수정</h3>
    <label>날짜 <input type="date" name="date" required></label>
    <label>출근 <input type="time" name="in" required></label>
    <label>퇴근 <input type="time" name="out"></label>
    <label><input type="checkbox" name="overnight"> 퇴근이 다음날 (야근)</label>
    <menu>
      <button value="delete" id="edit-delete" formnovalidate>삭제</button>
      <button value="cancel" formnovalidate>취소</button>
      <button value="save" id="edit-save">저장</button>
    </menu>
  </form>
</dialog>

<script type="module" src="app.js"></script>
</body>
</html>
```

- [ ] **Step 2: 수동 검증 — 정적 확인**

Run: `cd /Users/imina/Project/punch && python3 -m http.server 8765` (백그라운드)
브라우저(또는 browse 도구)로 `http://localhost:8765` 열기.
Expected: 파란 [출근 🌅] 큰 버튼, 월 내비게이션, 하단 버튼 4개가 보임. app.js가 아직 없어 404 콘솔 에러 1건은 정상(다음 태스크에서 해소). 375px 폭에서 가로 스크롤 없음.

- [ ] **Step 3: 커밋**

```bash
git add index.html
git commit -m "feat: 단일 페이지 마크업과 모바일 우선 스타일"
```

---

### Task 5: DOM 연결 — 원탭 기록 / 실행취소 / 월 리스트

**Files:**
- Create: `app.js`

**Interfaces:**
- Consumes: Task 1~2의 punch.js 함수 전부, Task 4의 DOM id
- Produces: `openEdit(rec|null)`은 이 태스크에서 빈 함수가 아니라 **Task 6에서 처음 정의**한다. Task 5의 리스트 행에는 아직 클릭 핸들러를 달지 않는다

- [ ] **Step 1: app.js 작성**

```js
import * as P from './punch.js';

const KEY = 'punch:v1';
const $ = (id) => document.getElementById(id);

let records = load();
let viewMonth = P.monthKey(P.todayStr(new Date()));
let undoSnapshot = null;
let undoTimer = null;

function load() {
  try {
    return JSON.parse(localStorage.getItem(KEY)) ?? [];
  } catch {
    return []; // 깨진 데이터는 빈 상태로 시작 (백업 복원으로 되살릴 수 있음)
  }
}

function setRecords(next, { undoable = false } = {}) {
  if (undoable) {
    undoSnapshot = records;
    clearTimeout(undoTimer);
    $('undo-bar').hidden = false;
    undoTimer = setTimeout(() => {
      $('undo-bar').hidden = true;
      undoSnapshot = null;
    }, 3000);
  }
  records = next;
  localStorage.setItem(KEY, JSON.stringify(records));
  render();
}

$('undo-btn').onclick = () => {
  if (undoSnapshot) {
    records = undoSnapshot;
    localStorage.setItem(KEY, JSON.stringify(records));
  }
  undoSnapshot = null;
  clearTimeout(undoTimer);
  $('undo-bar').hidden = true;
  render();
};

$('punch-btn').onclick = () => {
  const now = new Date();
  const today = P.todayStr(now);
  const hm = P.nowHM(now);
  const active = P.activeRecord(records, today);

  if (!active) {
    setRecords(P.punchIn(records, today, hm), { undoable: true });
    return;
  }
  if (active.date === today) {
    setRecords(P.punchOut(records, today, hm), { undoable: true });
    return;
  }
  // 어제의 미퇴근 기록: 자정 넘긴 야근인지, 어제 퇴근을 깜빡한 건지 사용자에게 확인
  const asOvernight = confirm(
    `어제(${active.date}) ${active.in} 출근의 퇴근으로 기록할까요?\n` +
    `(취소하면 오늘 출근으로 기록되고, 어제 기록은 나중에 수정하면 됩니다)`
  );
  if (asOvernight) {
    setRecords(P.punchOut(records, active.date, hm, true), { undoable: true });
  } else {
    setRecords(P.punchIn(records, today, hm), { undoable: true });
  }
};

$('prev-month').onclick = () => { viewMonth = P.shiftMonth(viewMonth, -1); render(); };
$('next-month').onclick = () => { viewMonth = P.shiftMonth(viewMonth, 1); render(); };

function render() {
  const now = new Date();
  const today = P.todayStr(now);

  $('today-title').textContent = new Intl.DateTimeFormat('ko-KR', {
    month: 'long', day: 'numeric', weekday: 'short',
  }).format(now);

  const active = P.activeRecord(records, today);
  const todayRec = P.getRecord(records, today);
  const btn = $('punch-btn');
  if (active) {
    btn.textContent = '퇴근 🌇';
    btn.className = 'out';
    btn.disabled = false;
    $('today-sub').textContent =
      `${active.in} 출근함${active.date !== today ? ' (어제)' : ''}`;
  } else if (todayRec?.out) {
    btn.textContent = `오늘 ${P.formatDuration(P.durationMinutes(todayRec))} 근무 완료 ✅`;
    btn.className = 'done';
    btn.disabled = true;
    $('today-sub').textContent = `${todayRec.in} ~ ${todayRec.out}`;
  } else {
    btn.textContent = '출근 🌅';
    btn.className = 'in';
    btn.disabled = false;
    $('today-sub').textContent = '';
  }

  const [y, m] = viewMonth.split('-');
  $('month-title').textContent = `${y}년 ${Number(m)}월`;

  const list = $('record-list');
  list.replaceChildren();
  for (const r of P.monthRecords(records, viewMonth)) {
    const li = document.createElement('li');
    const [, mm, dd] = r.date.split('-');
    const outText = r.out ?? (P.missingOut(r, today) ? '퇴근 미입력' : '근무 중');
    li.innerHTML =
      `<span class="d">${Number(mm)}/${Number(dd)}</span>` +
      `<span class="t">${r.in} ~ ${r.out ? r.out : `<em class="miss">${outText}</em>`}</span>` +
      `<span class="dur">${P.formatDuration(P.durationMinutes(r))}</span>`;
    list.appendChild(li);
  }

  $('month-total').textContent =
    `이번 달 합계: ${P.formatDuration(P.monthTotalMinutes(records, viewMonth)) || '0h 00m'}`;
}

render();
```

- [ ] **Step 2: 수동 검증 — 핵심 플로우**

서버 실행 중(`python3 -m http.server 8765`) 상태에서 `http://localhost:8765` 열고:
1. [출근] 탭 → 버튼이 주황 [퇴근]으로 바뀌고 "HH:MM 출근함" 표시, "기록됨 / 실행취소" 바 3초 표시
2. [실행취소] 탭 → [출근] 상태로 복귀
3. 다시 [출근] → [퇴근] 탭 → "오늘 0h 00m 근무 완료 ✅" + 리스트에 오늘 행 + 합계 표시
4. 새로고침 → 상태 유지 (localStorage)
5. ← → 월 이동 동작, 빈 달은 "0h 00m"
6. 콘솔 에러 0건

Expected: 전부 통과.

- [ ] **Step 3: 커밋**

```bash
git add app.js
git commit -m "feat: 원탭 출퇴근, 실행취소, 월 리스트/합계 렌더"
```

---

### Task 6: 수정/추가/삭제 다이얼로그 + 야근 확인

**Files:**
- Modify: `app.js` (끝에 추가 + 리스트 행에 클릭 연결)

**Interfaces:**
- Consumes: Task 2의 `upsertRecord`/`deleteRecord`, Task 1의 `isOvernightCandidate`, Task 4의 `edit-dialog`/`edit-form`
- Produces: `openEdit(rec|null)` — rec이 있으면 수정 모드, null이면 추가 모드

- [ ] **Step 1: 리스트 행에 클릭 연결** — Task 5의 render() 안 `list.appendChild(li);` 바로 앞에 한 줄 추가

```js
    li.onclick = () => openEdit(r);
```

- [ ] **Step 2: 다이얼로그 코드 추가** — `app.js` 끝(render() 호출 앞)에 추가

```js
const dlg = $('edit-dialog');
const form = $('edit-form');
let editingDate = null; // null이면 추가 모드

function openEdit(rec) {
  editingDate = rec?.date ?? null;
  $('edit-title').textContent = rec ? '기록 수정' : '기록 추가';
  $('edit-delete').hidden = !rec;
  form.elements.date.value = rec?.date ?? P.todayStr(new Date());
  form.elements.in.value = rec?.in ?? '09:00';
  form.elements.out.value = rec?.out ?? '';
  form.elements.overnight.checked = !!rec?.overnight;
  dlg.showModal();
}

$('add-btn').onclick = () => openEdit(null);

dlg.onclose = () => {
  const f = form.elements;
  if (dlg.returnValue === 'delete' && editingDate) {
    if (confirm('이 기록을 삭제할까요?')) setRecords(P.deleteRecord(records, editingDate));
    return;
  }
  if (dlg.returnValue !== 'save') return;

  const rec = {
    date: f.date.value,
    in: f.in.value,
    out: f.out.value || null,
    overnight: f.overnight.checked,
  };
  // 자정 넘긴 야근: 퇴근이 출근보다 이르면 확인
  if (rec.out && !rec.overnight && P.isOvernightCandidate(rec.in, rec.out)) {
    rec.overnight = confirm('퇴근이 출근보다 이릅니다. 다음날 퇴근(야근)으로 계산할까요?');
  }
  let next = records;
  if (editingDate && editingDate !== rec.date) next = P.deleteRecord(next, editingDate);
  setRecords(P.upsertRecord(next, rec));
};
```

- [ ] **Step 3: 수동 검증**

`http://localhost:8765`에서:
1. 리스트의 오늘 행 탭 → 다이얼로그에 기존 값 채워져 열림 → 출근을 08:30으로 고치고 [저장] → 리스트와 합계 갱신
2. [+ 기록 추가] → 어제 날짜, 09:00~18:00 입력 [저장] → 어제 행 추가됨
3. 어제 행 탭 → [삭제] → confirm 후 사라짐
4. 추가 모드에서 출근 22:00 / 퇴근 01:00 [저장] → "다음날 퇴근(야근)으로 계산할까요?" confirm 표시, 확인 시 3h 00m
5. [취소]는 아무 변화 없음. 날짜 비우고 [저장] → HTML 검증에 막힘
6. 콘솔 에러 0건

Expected: 전부 통과.

- [ ] **Step 4: 커밋**

```bash
git add app.js
git commit -m "feat: 기록 수정/추가/삭제 다이얼로그와 야근 확인"
```

---

### Task 7: CSV 다운로드 + 백업/복원

**Files:**
- Modify: `app.js` (끝에 추가)

**Interfaces:**
- Consumes: Task 3의 `toCSV`/`toBackup`/`fromBackup`, Task 4의 `csv-btn`/`backup-btn`/`restore-btn`/`restore-input`

- [ ] **Step 1: 코드 추가** — `app.js` 끝(render() 호출 앞)에 추가

```js
function download(name, text, type) {
  const a = document.createElement('a');
  a.href = URL.createObjectURL(new Blob([text], { type }));
  a.download = name;
  a.click();
  URL.revokeObjectURL(a.href);
}

$('csv-btn').onclick = () => download('punch.csv', P.toCSV(records), 'text/csv');
$('backup-btn').onclick = () => download('punch-backup.json', P.toBackup(records), 'application/json');
$('restore-btn').onclick = () => $('restore-input').click();
$('restore-input').onchange = async (e) => {
  const file = e.target.files[0];
  if (!file) return;
  try {
    const restored = P.fromBackup(await file.text());
    if (confirm(`${restored.length}건의 기록으로 교체할까요?\n현재 기록은 사라집니다.`)) {
      setRecords(restored);
    }
  } catch (err) {
    alert(`복원 실패: ${err.message}`);
  }
  e.target.value = '';
};
```

- [ ] **Step 2: 수동 검증**

`http://localhost:8765`에서 (기록 2건 이상 있는 상태로):
1. [CSV 내보내기] → punch.csv 다운로드, 열어서 헤더+행 확인 (숫자 minutes 포함)
2. [백업] → punch-backup.json 다운로드
3. 오늘 기록 하나 삭제 → [복원] → 방금 받은 json 선택 → confirm 후 기록이 되돌아옴
4. 텍스트 파일에 `{{{` 저장해 복원 시도 → "복원 실패: JSON 파일이 아닙니다" alert
5. 콘솔 에러 0건

Expected: 전부 통과.

- [ ] **Step 3: 커밋**

```bash
git add app.js
git commit -m "feat: CSV 내보내기와 JSON 백업/복원"
```

---

### Task 8: README + GitHub Pages 배포 + 최종 검증

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: 완성된 앱 전체

- [ ] **Step 1: README.md 작성**

```markdown
# punch 🕘

출근/퇴근 버튼 하나로 기록하고 월 근무시간을 자동 합산하는 웹페이지.
서버 없음, 로그인 없음, 설치 없음 — 데이터는 내 브라우저에만 저장됩니다.

**사용하기:** https://<GitHub아이디>.github.io/punch/

## 사용법

1. 아침에 [출근 🌅] 탭, 저녁에 [퇴근 🌇] 탭. 끝.
2. 깜빡했으면 기록 줄을 탭해서 시간을 고치거나, [+ 기록 추가]로 지난 날짜를 채우세요.
3. 월말엔 [CSV 내보내기]로 엑셀에서 열 수 있습니다.

## 깜빡 방지 팁

이 앱은 알림을 보내지 않습니다 (웹 알림은 신뢰도가 낮아 일부러 뺐습니다).
대신 폰 기본 알람을 두 개 등록하세요: 평일 오전 9시 "출근 찍기", 오후 6시 "퇴근 찍기".
폰 홈 화면에 이 페이지를 추가하면 앱처럼 쓸 수 있습니다.

## 데이터는 어디에?

전부 브라우저 localStorage에만 있습니다. 서버로 전송되지 않습니다.
따라서 **기기마다 기록이 따로**이고, 브라우저 데이터를 지우면 기록도 지워집니다.
가끔 [백업]으로 JSON을 받아두세요. [복원]으로 다른 기기에 옮길 수 있습니다.

## 개발

의존성 없음. 테스트: `node --test test/*.test.js` · 로컬 실행: `python3 -m http.server 8765`
```

- [ ] **Step 2: 전체 테스트 최종 확인**

Run: `node --test test/*.test.js`
Expected: PASS — 20 tests

- [ ] **Step 3: 커밋**

```bash
git add README.md
git commit -m "docs: README (사용법, 알람 팁, 데이터 안내)"
```

- [ ] **Step 4: GitHub 저장소 생성 + 푸시** (외부 공개 — 사용자 확인 후 실행)

```bash
cd /Users/imina/Project/punch
gh repo create punch --public --source . --push
```

Expected: `https://github.com/<아이디>/punch` 생성, main 푸시 완료.

- [ ] **Step 5: GitHub Pages 활성화**

```bash
gh api "repos/{owner}/punch/pages" -X POST -f "source[branch]=main" -f "source[path]=/"
```

Expected: HTTP 201. 1~2분 후 `https://<아이디>.github.io/punch/` 접속 가능.
(이미 활성화돼 있으면 409 — 그 경우 무시하고 진행)

- [ ] **Step 6: 배포 검증**

```bash
sleep 90 && curl -sI "https://$(gh api user -q .login).github.io/punch/" | head -1
```

Expected: `HTTP/2 200`. 그 후 브라우저로 실제 접속해 출근 탭 → 새로고침 → 상태 유지 확인.

- [ ] **Step 7: 폰 최종 검증 (사용자 손에서)**

사용자에게 안내: 폰에서 URL 접속 → 출근 탭 → 퇴근 탭 → 시간 수정 → CSV 열기.
스펙의 완성 기준: 이 흐름이 막힘없이, 원탭은 3초 안에.

- [ ] **Step 8: README의 URL 갱신 후 마무리 커밋**

README.md의 `<GitHub아이디>` 를 실제 아이디로 치환.

```bash
git add README.md
git commit -m "docs: 실제 배포 URL로 갱신"
git push
```
